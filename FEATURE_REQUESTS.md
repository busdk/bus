# FEATURE_REQUESTS.md

- [x] Keep dispatcher `.env` loading generic: a working-directory `.env` file must be parsed into the child process environment without filtering to Bus-specific variable names, and process environment values must keep precedence.
