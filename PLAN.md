- [x] Register in-process Tx-capable runners for real modules so `provider=fs` can run outside tests.
- [ ] Optimize `withBusfileEnv`/`upsertEnv` to avoid rebuilding and rescanning the full env slice three times per command execution (`internal/dispatch/run.go`).
- [ ] In `executeFSUnit`, compute batch journal file lists once per unit instead of calling `uniqueCommandFiles(commands)` for each journal state transition (`internal/dispatch/run.go`).
- [ ] Reduce `provider=fs` per-command overhead by removing full workspace copy + full-tree diff/merge in `runModuleViaTempWorkspaceAndMerge` and using a more incremental change-capture strategy (`internal/dispatch/run.go`).
- [ ] Replace linear tombstone prefix scans in `FS.isDeleted` with a path-indexed structure to prevent lookup cost from growing with delete count (`internal/txfs/txfs.go`).
- [ ] Cache/deduplicate `lookPathEnv` resolution per target during busfile preflight/dispatch to avoid repeated PATH scans for the same command (`internal/dispatch/run.go`).
- [ ] Reduce `preflightDispatchTargets` wide-PATH overhead for many unique targets by building a single preflight executable index (or equivalent prepared PATH search state) instead of re-splitting/scanning PATH for every command lookup (`internal/dispatch/run.go`; benchmarked by `BenchmarkPreflightDispatchTargetsUniqueLookupsWidePath` in `internal/dispatch/run_bench_test.go` at ~3.47ms/op, ~1.20MB/op, 8388 allocs/op).
- [ ] Reduce `FS.markDelete` scan cost (currently iterates full `changes` map per delete) by using an index that can remove descendants without O(n) full-map walks (`internal/txfs/txfs.go`).
- [ ] Reduce repeated `FS.OpenFile` path-setup overhead for hot append/update loops by avoiding redundant parent directory materialization and per-call path bookkeeping when the target is already materialized (`internal/txfs/txfs.go`; benchmarked by `BenchmarkOpenFileRepeatedExistingPath` in `internal/txfs/txfs_bench_test.go` at ~107-127us/op, 9 allocs/op).
- [ ] Reduce `FS.Open`/`FS.Stat` unchanged-file read overhead by avoiding per-call overlay existence `os.Stat` probes on base-only paths (prefer a fast path keyed by overlay change tracking before filesystem fallback) while preserving overlay precedence and tombstone semantics (`internal/txfs/txfs.go`; benchmarked by `BenchmarkOpenReadExistingPath` at ~100us/op, 8 allocs/op and `BenchmarkStatExistingPath` at ~3.2-3.4us/op, 7 allocs/op in `internal/txfs/txfs_bench_test.go`).
- [ ] Reduce allocation-heavy busfile command validation by replacing repeated `big.Rat` parsing in `validateJournalAdd`/`validateBankAddTransactions` with a lower-allocation decimal parser strategy while preserving exact money semantics (`internal/dispatch/run.go`, benchmarked in `internal/dispatch/run_bench_test.go`).
- [ ] Reduce `.bus` preflight parser allocation churn by optimizing `tokenizeBusLine` and `collectBusfileCommands` (current benchmark shape: `BenchmarkTokenizeBusLineJournalAdd` ~15 allocs/op and `BenchmarkCollectBusfileCommands` ~8709 allocs/op for 512-line files) while preserving syntax and diagnostics behavior (`internal/dispatch/run.go`, benchmarks in `internal/dispatch/run_bench_test.go`).
- [ ] Reduce subcommand discovery overhead in `listSubcommands` for dense PATH directories by minimizing per-entry filesystem checks and allocation churn (current benchmark shape: `BenchmarkListSubcommandsDensePath` ~1.3ms/op, ~302KB/op, ~3458 allocs/op) while preserving deterministic PATH-order dedupe and sorted output (`internal/dispatch/run.go`, benchmarked in `internal/dispatch/run_bench_test.go`).
- [ ] Reduce unchanged-tree merge cost in `mergeWorkspaceChangesToTxFS` by avoiding full byte-by-byte `filesEqual` comparisons for unchanged files (for example, metadata/hash-assisted short-circuiting while preserving correctness), benchmarked by `BenchmarkMergeWorkspaceChangesToTxFSUnchangedTree` in `internal/dispatch/run_bench_test.go` at ~24.6ms/op for 64x4KiB files and ~194ms/op for 128x64KiB files.
- [ ] Reduce `FS.Commit` large-change overhead by avoiding full-map key collection/sort and per-file commit syscall churn on unchanged commit ordering assumptions (`internal/txfs/txfs.go`; benchmarked by `BenchmarkCommitReplaceManyFiles` in `internal/txfs/txfs_bench_test.go` at ~45.8ms/op for 10 files, ~682ms/op for 100 files, and ~3.48s/op for 500 files).
