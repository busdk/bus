# Roadmap 0.1.1 — CLI: Skeleton (No Commands Yet)

## Goal
Add the minimal CLI scaffolding (argument parsing + dispatch) without implementing any specific commands yet.

## Progress
planned

## Non-goals
- Implement any CLI commands.

## User-visible outcomes
- None (CLI-only scaffolding).

## Related previous versions
- `0.1.1` (composition model exists)

## New or changed interfaces (exact names + boundaries)
CLI transport contract (canonical):
- `docs/spec/transports-cli.md`

## Module responsibilities (what belongs in a module vs core)
- **CLI**: command-line parsing and dispatch only.
- **Core**: remains transport-agnostic.

## Data model changes
None.

## API/CLI changes
None.

## Configuration knobs introduced
None.

## Documentation goals
- Clarify that CLI is a thin wrapper and commands are added one patch at a time.

## Implementation goals
- Implement CLI entrypoint and scaffolding needed to add commands.

## Automated testing goals
- CLI-layer tests for basic invocation and help output (if present).

## Acceptance criteria
- CLI scaffolding exists without adding any command behavior.


