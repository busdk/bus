# Roadmap 0.0.10 — Core: Schema Registry (Manifest `units[]`)

## Goal
Implement schema registration and lookup by name via manifest `units[]` (YAML manifest only).

## Progress
planned

## Non-goals
- CLI schema commands.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.4` (manifest exists)
- `0.0.9` (schema validation exists)

## New or changed interfaces (exact names + boundaries)
Schema registration rules (canonical):
- `docs/spec/schemas.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: updates `units[]` deterministically and enforces uniqueness.

## Data model changes
Manifest `units[]` populated with:
- `name`
- `path`

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify manifest `units[]` is YAML-only at this stage.

## Implementation goals
- Implement schema registry backed by manifest `units[]`.

## Automated testing goals
- Unit tests for name/path uniqueness and deterministic manifest updates.

## Acceptance criteria
- Registering schemas updates manifest deterministically and enforces uniqueness constraints.


