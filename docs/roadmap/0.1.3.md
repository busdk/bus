# Roadmap 0.1.3 — Core: Tenancy Defaults

## Goal
Implement tenant defaulting rules (tenant id is mandatory on every operation), backend-agnostic.

## Progress
planned

## Non-goals
- CLI flag wiring (CLI can pass tenant id; core owns defaulting rules).

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.1.1`

## New or changed interfaces (exact names + boundaries)
Tenant rules (canonical):
- `docs/spec/tenancy.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: resolves tenant id and enforces tenant-scoped operations.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
- Manifest: `tenants.default` (optional; otherwise `"default"`).

## Documentation goals
- Ensure tenant rules are documented in `docs/spec/tenancy.md`.

## Implementation goals
- Implement tenant resolution order deterministically.

## Automated testing goals
- Unit tests for tenant resolution order.

## Acceptance criteria
- Tenant selection matches `docs/spec/tenancy.md` exactly.


