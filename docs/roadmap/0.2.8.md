# Roadmap 0.2.8 — CLI Command: `bus <schema> list`

## Goal
Add the `bus [schema] <schema> list` CLI command as a thin wrapper over core unit list.

## Progress
planned

## Non-goals
- Any new list logic in core.

## User-visible outcomes
- `bus <schema> list` same as `bus schema <schema> list`
- `bus schema <schema> list` prints unit ids deterministically.

## Related previous versions
- `0.2.7` (core unit list exists)
- `0.1.1` (CLI scaffolding exists)

## New or changed interfaces (exact names + boundaries)
None.

## Module responsibilities (what belongs in a module vs core)
- **Core**: owns unit list operation.
- **CLI**: prints results.

## Data model changes
None.

## API/CLI changes
- `bus schema <SCHEMA_NAME> list`

## Configuration knobs introduced
None.

## Documentation goals
- Document this command as a CLI wrapper over core.

## Implementation goals
- Implement CLI wiring only.

## Automated testing goals
- CLI-layer tests for deterministic ordering.

## Acceptance criteria
- CLI delegates to core; list logic is not duplicated in CLI.
