# Roadmap 0.2.3 — Core: Schema Init Operation (Create + Register)

## Goal
Implement a core “schema init” operation that creates a YAML schema document and registers it in the YAML manifest atomically.

## Progress
planned

## Non-goals
- CLI schema init command (separate patch).

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.2.1` (schema validation)
- `0.2.2` (schema registry)

## New or changed interfaces (exact names + boundaries)
Schema property spec and document format (canonical):
- `docs/spec/schemas.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: constructs schema docs and updates manifest atomically.

## Data model changes
Creates schema file and updates manifest `units[]`.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify schema init is YAML-only at this stage.

## Implementation goals
- Implement atomic create+register behavior.

## Automated testing goals
- Unit tests proving schema+manifest updates are atomic and deterministic.

## Acceptance criteria
- Schema init operation is transport-agnostic and deterministic.


