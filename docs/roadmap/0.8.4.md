# Roadmap 0.5.4 — HTTP Feature: x402 Normalize Into Transaction

## Goal
Normalize parsed x402 artifacts into the micropayments transaction shape and write a transaction record (hosted HTTP feature logic + core state primitives).

## Progress
planned

## Non-goals
- CLI command wiring.
- Uniqueness/double-booking prevention (separate patch).

## User-visible outcomes
- None (feature-only).

## Related previous versions
- `0.5.3` (header parsing exists)
- `0.4.1` (micropayments model exists)

## New or changed interfaces (exact names + boundaries)
Normalization rules (canonical):
- `docs/spec/x402.md`
- `docs/spec/micropayments.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: unit creation primitives + state backend operations.
- **Hosted HTTP**: mapping parsed artifacts → normalized transaction fields.

## Data model changes
None (stored as unit data).

## API/CLI changes
None (feature-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure normalization mapping rules are documented in `docs/spec/x402.md`.

## Implementation goals
- Implement normalization mapping and transaction record creation.

## Automated testing goals
- Add unit tests proving normalization is deterministic for fixed inputs.

## Acceptance criteria
- Hosted HTTP feature can write a normalized transaction deterministically (using core unit/state primitives).
