# Roadmap 0.0.41 — Core: Hosted Facilitator Contract (HTTP + OpenAPI)

## Goal
Define the hosted facilitator as a thin HTTP front-end over core operations, with a published OpenAPI contract (core docs/spec only).

## Progress
planned

## Non-goals
- Implement the HTTP server.
- Add any CLI behavior.

## User-visible outcomes
- None (developer-facing contract step).

## Related previous versions
- `0.0.39` (x402 ingest endpoint exists)
- `0.0.33` (micropayments report operation exists)

## New or changed interfaces (exact names + boundaries)
HTTP + OpenAPI contract (canonical):
- `docs/spec/hosted-facilitator-http.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: request context creation + enforcement hooks.
- **Implementations**: provide HTTP handlers and OpenAPI schema metadata; wired in code (optionally selected via a provider registry by id).

## Data model changes
None.

## API/CLI changes
None (contract-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure HTTP surface and OpenAPI requirements are documented in `docs/spec/hosted-facilitator-http.md`.

## Implementation goals
- None (contract-only patch).

## Automated testing goals
- None (contract-only patch).

## Acceptance criteria
- Contract is explicit: HTTP is thin; OpenAPI is required; `/v1/...` paths are versioned.
