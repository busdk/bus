# Roadmap 0.0.3 — Core: Manifest Discovery (YAML Only)

## Goal
Implement deterministic manifest discovery, supporting only YAML manifest candidates (`bus.yml` / `bus.yaml`) at this stage.

## Progress
planned

## Non-goals
- TOML/JSON manifest support.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.2` (YAML codec exists)

## New or changed interfaces (exact names + boundaries)
Workspace discovery rules (canonical):
- `docs/spec/workspace-manifest.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: implements manifest candidate discovery and ambiguity handling.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify that only YAML manifests are supported in this early version.

## Implementation goals
- Implement manifest discovery for `bus.yml`/`bus.yaml` with deterministic errors.

## Automated testing goals
- Unit tests for none/one/multiple candidate behavior and deterministic error output.

## Acceptance criteria
- Manifest discovery is deterministic and YAML-only.


