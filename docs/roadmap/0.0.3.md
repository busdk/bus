# Roadmap 0.0.3 — Core: YAML Codec Only

## Goal
Implement YAML decoding/encoding deterministically. YAML is the only supported config format at this stage.

## Progress
planned

## Non-goals
- JSON/TOML codecs.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.2`

## New or changed interfaces (exact names + boundaries)
YAML codec rules (canonical):
- `docs/spec/codec-yaml.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: provides YAML codec implementation.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure deterministic YAML rules are documented in `docs/spec/codec-yaml.md`.

## Implementation goals
- Implement YAML codec with deterministic encoding.

## Automated testing goals
- Unit tests for deterministic YAML output and decode/encode round-trip.

## Acceptance criteria
- YAML encoding is deterministic across runs.


