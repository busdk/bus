# Roadmap 0.0.34 — HTTP Feature: x402 402 Generation Operation

## Goal
Implement x402 402 body generation as hosted HTTP feature logic (built on core primitives).

## Progress
planned

## Non-goals
- CLI command wiring (separate patch).

## User-visible outcomes
- None (feature-only).

## Related previous versions
- `0.0.32` (micropayments model exists; x402 will normalize into it later)

## New or changed interfaces (exact names + boundaries)
x402 wire/config definitions (canonical):
- `docs/spec/x402.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: unit read/query primitives + state backend access.
- **Hosted HTTP**: reads x402 config units and generates deterministic 402 JSON bodies.

## Data model changes
Introduces recommended config schemas as normal schemas/units:
- `service`, `x402_policy`, `x402_accept`

## API/CLI changes
None (feature-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure x402 config + wire body is documented in `docs/spec/x402.md`.

## Implementation goals
- Implement deterministic 402 body generation from config.

## Automated testing goals
- Add unit tests for deterministic 402 output given fixed config state.

## Acceptance criteria
- Operation is deterministic and tenant-scoped without any CLI code.
