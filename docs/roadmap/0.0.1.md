# Roadmap 0.0.1 — Documentation Conventions, Vocabulary, and Non‑Negotiables

## Goal
Define how the docs are organized, how to use the roadmap, the canonical vocabulary rules, and the non‑negotiable architectural principles.

## Progress
**done** (documentation-only version)

## Non-goals
- Specify detailed feature behavior (later roadmap versions).

## User-visible outcomes
- A single “start here” roadmap entry that explains how to implement Bus incrementally.

## New or changed interfaces
None.

## Module responsibilities (what belongs in a module vs core)
Boundary rule for all roadmap versions:
- **Bus Core**: orchestration only (module runtime, config validation, routing, cross-cutting enforcement hooks).
- **Modules**: all business logic behind core-owned interfaces.

## Data model changes
None.

## API/CLI changes
None.

## Configuration knobs introduced
None.

## Documentation goals
- Establish the roadmap/spec split (`docs/roadmap` vs `docs/spec`) and the SemVer filename rule.
- Point to the single sources of truth for vocabulary and principles.

## Implementation goals
- None (documentation-only version).

## Automated testing goals
- None (documentation-only version).

## Acceptance criteria
- `docs/README.md` links to this file as the start point.
- All roadmap files live under `docs/roadmap/`.
- Filenames match SemVer versions: `X.Y.Z.md`.
- The intended implementation order is the **SemVer sort order** (for this repo: `0.0.1`, `0.0.2`, …).

---

## SemVer roadmap rule (machine-parsable)
Each file under `docs/roadmap/{VERSION}.md` is the specification for Bus version `{VERSION}`.

Examples:
- `docs/roadmap/0.0.1.md` ⇒ Bus `0.0.1`
- `docs/roadmap/0.1.3.md` ⇒ Bus `0.1.3`

## Spec vs roadmap
- **Roadmap files (`docs/roadmap/`)**: step-by-step implementation plan. Each version is one small increment with acceptance criteria.
- **Spec files (`docs/spec/`)**: reusable definitions (one concept per file) referenced by roadmap steps.

## Progress field (binding)
Each roadmap file MUST include a `## Progress` section with one of:
- `planned`
- `in_progress`
- `done`

Definition:
- **planned**: specified but not implemented/verified yet
- **in_progress**: actively being implemented
- **done**: implementation + automated tests (if applicable) are complete for this version’s scope

## Vocabulary (single source)
Use the canonical terms defined in:
- `docs/spec/vocabulary.md`

## Non-negotiable architectural principles (single source)
These principles are binding for every roadmap step:
- `docs/spec/principles.md`


