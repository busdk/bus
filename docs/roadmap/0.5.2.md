# Roadmap 0.5.2 — HTTP: Server Skeleton + OpenAPI Publishing

## Goal
Implement the hosted facilitator (“busd”) HTTP server skeleton that:
- serves versioned routes under `/v1/...`
- publishes OpenAPI at `/openapi.json`
- maps request context (tenant, request id) into core operations

## Progress
planned

## Non-goals
- Implement any business endpoints beyond a health/version endpoint.
- Implement authn/authz (placeholder hooks only).

## User-visible outcomes
- A running HTTP server that serves `/openapi.json` and a minimal `/v1/health` endpoint.

## Related previous versions
- `0.5.1` (hosted facilitator contract exists)

## Canonical definitions
- `docs/spec/hosted-facilitator-http.md`

## Implementation goals
- Add HTTP server entrypoint and routing layer.
- Add OpenAPI generation/publication mechanism.
- Ensure tenant selection rules match `docs/spec/tenancy.md`.

## Acceptance criteria
- OpenAPI is served and versioned paths exist.
- Request context is created deterministically and passed into core operations.


