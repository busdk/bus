# Roadmap 0.6.1 — Core: Export/Settlement Extension Point Interfaces

## Goal
Define export and settlement extension point interfaces as core-owned contracts (interfaces only).

## Progress
planned

## Non-goals
- Implement exporters/proposers.
- Add CLI commands for exporters.

## User-visible outcomes
- None (developer-facing core step).

## Related previous versions
- `0.1.1` (interfaces + built-in implementation selection exists)

## New or changed interfaces (exact names + boundaries)
Extension point interfaces (canonical):
- `docs/spec/exports-and-settlement.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: owns interfaces, validates config, and enforces cross-cutting concerns.
- **Implementations**: provide exporters/proposers/views and own their output formats; wired in code (optionally selected via a provider registry by id).

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure interfaces are documented in `docs/spec/exports-and-settlement.md`.

## Implementation goals
- None (interfaces-only patch).

## Automated testing goals
- None (interfaces-only patch).

## Acceptance criteria
- Interfaces are stable and implementation-friendly (implement + wire; no per-feature core branching).
