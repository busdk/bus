# Roadmap 0.0.9 — Core: Schema Validation

## Goal
Implement schema parsing + validation rules (YAML-only for schema documents at this stage).

## Progress
planned

## Non-goals
- JSON/TOML schema documents.
- CLI schema commands.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.2` (YAML codec exists)

## New or changed interfaces (exact names + boundaries)
Schema rules (canonical):
- `docs/spec/schemas.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: validates schema documents against canonical rules.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify schemas are YAML-only at this stage.

## Implementation goals
- Implement schema validator (primary id rules, type rules, property uniqueness).

## Automated testing goals
- Unit tests covering binding schema rules.

## Acceptance criteria
- Schema validation is deterministic and matches `docs/spec/schemas.md`.


