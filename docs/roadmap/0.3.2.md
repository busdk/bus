# Roadmap 0.3.2 — Core: Unit Validation (Refs)

## Goal
Enforce typed reference existence validation (`ref:<schema>`) during unit creation (core-only).

## Progress
planned

## Non-goals
- CLI changes.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.3.1` (types exist so ref fields are typed)

## New or changed interfaces (exact names + boundaries)
Reference semantics (canonical):
- `docs/spec/relations.md`
- `docs/spec/units.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: enforces reference existence checks.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Document ref validation error behavior.

## Implementation goals
- Implement `ref:<schema>` existence checks during unit creation.

## Automated testing goals
- Unit tests for ref validation across schemas.

## Acceptance criteria
- Ref validation is deterministic.
