# Roadmap 0.3.4 — Docs: Ledger / Transactions Pattern (Append‑Only)

## Goal
Codify the ledger/transactions pattern as “create-only units”, without adding a special transaction subsystem.

## Progress
planned

## Non-goals
- Implement invoices, settlement, or billing rules.

## User-visible outcomes
- None (docs-only patch).

## Related previous versions
- `0.2.3` (schemas are creatable/registrable)
- `0.2.7` (units list/show exist)

## New or changed interfaces (exact names + boundaries)
Ledger/transactions pattern (canonical):
- `docs/spec/ledger-transactions.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: remains orchestration/storage only.
- **Modules**: own reporting/exports/settlement proposals on top of ledger units.

## Data model changes
None.

## API/CLI changes
None.

## Configuration knobs introduced
None.

## Documentation goals
- Ensure append-only ledger pattern is documented in `docs/spec/ledger-transactions.md`.

## Implementation goals
- None (docs-only patch).

## Automated testing goals
- None (docs-only patch).

## Acceptance criteria
- Docs make it unambiguous that corrections are new records, not edits.
