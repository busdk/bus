# Roadmap 0.2.5 — Core: Unit Create Operation

## Goal
Implement the core unit creation operation using the filesystem `.bus` state backend.

## Progress
planned

## Non-goals
- CLI unit commands.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.1.4` (filesystem state backend exists)
- `0.2.2` (schema registry exists)

## New or changed interfaces (exact names + boundaries)
Unit rules (canonical):
- `docs/spec/units.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: implements unit creation against the selected state backend.

## Data model changes
Backend-defined internal state under `.bus/` (filesystem backend).

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify unit storage uses filesystem backend at this stage.

## Implementation goals
- Implement unit create with UUID autogeneration behavior.

## Automated testing goals
- Unit tests for UUID autogeneration and deterministic behavior.

## Acceptance criteria
- Core unit create is deterministic and tenant-scoped.


