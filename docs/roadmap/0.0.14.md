# Roadmap 0.0.14 — CLI Command: `bus <schema> add`

## Goal
Add the `bus <schema> add ...` CLI command as a thin wrapper over core unit create.

## Progress
planned

## Non-goals
- Any new unit creation logic in core.

## User-visible outcomes
- `bus <schema> add key=value ...` creates a unit and prints its primary id.

## Related previous versions
- `0.0.13` (core unit create exists)
- `0.0.5` (CLI scaffolding exists)

## New or changed interfaces (exact names + boundaries)
CLI parsing rules (canonical):
- `docs/spec/transports-cli.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: owns unit create operation.
- **CLI**: parses args and calls core.

## Data model changes
None (delegates to core).

## API/CLI changes
- `bus <SCHEMA_NAME> add key=value key2=value2 ...`

## Configuration knobs introduced
None.

## Documentation goals
- Document this command as YAML-only config + filesystem state backend at this stage.

## Implementation goals
- Implement CLI wiring only.

## Automated testing goals
- CLI-layer tests for parsing and deterministic errors.

## Acceptance criteria
- CLI delegates to core; create logic is not duplicated in CLI.


