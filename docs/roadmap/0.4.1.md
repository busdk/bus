# Roadmap 0.4.1 — Core: Micropayments Report Operation

## Goal
Define a core-owned micropayments report operation (transport-agnostic) and provide a built-in module implementation that performs deterministic aggregation over transactions.

## Progress
planned

## Non-goals
- CLI command wiring (separate patch).

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.2.7` (units list/show exist to query transactions)

## New or changed interfaces (exact names + boundaries)
Micropayments model requirements (canonical):
- `docs/spec/micropayments.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: owns the report operation interface (inputs/outputs, determinism contract).
- **Micropayments module (built-in)**: implements deterministic aggregation over normalized transactions.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure micropayments model is documented in `docs/spec/micropayments.md`.
  - Include concrete report examples of grouping/aggregation output.

## Implementation goals
- Implement deterministic aggregation over normalized transactions.

## Automated testing goals
- Add unit tests for deterministic grouping/output.

## Acceptance criteria
- Operation is deterministic without any CLI code.
