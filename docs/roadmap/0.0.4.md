# Roadmap 0.0.4 — Core: Workspace Init (Create YAML Manifest)

## Goal
Implement core workspace init operation that creates a YAML manifest (`bus.yml`) with minimal required fields.

## Progress
planned

## Non-goals
- CLI command wiring (separate patch).

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.3` (manifest discovery exists)

## New or changed interfaces (exact names + boundaries)
Manifest fields (canonical):
- `docs/spec/workspace-manifest.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: provides init operation callable from any transport.

## Data model changes
Creates `bus.yml` with:
- `kind: bus.manifest`
- `version: 1`
- `units: []`

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Clarify that init creates YAML manifest only at this stage.

## Implementation goals
- Implement core init operation with deterministic behavior if manifest exists.

## Automated testing goals
- Unit tests for init behavior and deterministic errors.

## Acceptance criteria
- Core init is deterministic and produces `bus.yml`.


