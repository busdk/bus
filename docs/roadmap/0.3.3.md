# Roadmap 0.3.3 — Core: Unit Validation (Uniqueness + `uniqueScope`)

## Goal
Implement schema-driven uniqueness constraints, including scoped uniqueness (`uniqueScope`) (core-only).

## Progress
planned

## Non-goals
- CLI changes.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.3.2` (refs exist so scope values can be trusted)

## New or changed interfaces (exact names + boundaries)
Uniqueness semantics (canonical):
- `docs/spec/schemas.md`
- `docs/spec/relations.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: state backend provides primitives needed to enforce uniqueness (backend-owned indexes allowed).
- **Core**: applies schema rules deterministically.

## Data model changes
Backend-defined (may require backend-owned indexes).

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Document uniqueness + scoped uniqueness semantics (single source: `docs/spec/relations.md`).

## Implementation goals
- Implement uniqueness enforcement for `unique` and `uniqueScope`.

## Automated testing goals
- Unit tests for global uniqueness and scoped uniqueness by tuple.

## Acceptance criteria
- Uniqueness behavior is deterministic.
