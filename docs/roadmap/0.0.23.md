# Roadmap 0.0.23 — Core: TOML Codec

## Goal
Add TOML support as an additional codec (deterministic encoding).

## Progress
planned

## Non-goals
- Make TOML required for the minimal path (YAML remains the default).

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.0.2` (YAML codec exists first)

## New or changed interfaces (exact names + boundaries)
TOML codec rules (canonical):
- `docs/spec/codec-toml.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: includes TOML codec implementation.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure TOML codec requirements are documented in `docs/spec/codec-toml.md`.

## Implementation goals
- Implement TOML deterministic encoding.

## Automated testing goals
- Unit tests for deterministic TOML output and decode/encode round-trip.

## Acceptance criteria
- TOML encoding is deterministic across runs.
