# Roadmap 0.3.1 — Core: Unit Validation (Required + Types)

## Goal
Enforce required fields and type checking during unit creation based on schema definitions (core-only).

## Progress
planned

## Non-goals
- CLI changes.

## User-visible outcomes
- None (core-only).

## Related previous versions
- `0.2.5` (core unit create exists)
- `0.2.1` (schema type system exists)

## New or changed interfaces (exact names + boundaries)
Validation rules (canonical):
- `docs/spec/schemas.md`
- `docs/spec/units.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: enforces required/type validation using schema definitions.

## Data model changes
None.

## API/CLI changes
None (core-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Document deterministic error behavior for type/required failures.

## Implementation goals
- Implement schema-driven type parsing and required-field checks for unit creation.

## Automated testing goals
- Unit tests covering each supported type and required/missing behavior.

## Acceptance criteria
- Validation matches `docs/spec/schemas.md` and `docs/spec/units.md`.
