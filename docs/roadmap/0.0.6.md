# Roadmap 0.0.6 — CLI Command: `bus init`

## Goal
Add the `bus init` CLI command as a thin wrapper over the core init operation.

## Progress
planned

## Non-goals
- Any new core init logic.

## User-visible outcomes
- `bus init` creates `bus.yml` deterministically.

## Related previous versions
- `0.0.4` (core init exists)
- `0.0.5` (CLI scaffolding exists)

## New or changed interfaces (exact names + boundaries)
None.

## Module responsibilities (what belongs in a module vs core)
- **Core**: owns init operation.
- **CLI**: parses args and calls core.

## Data model changes
None (delegates to core).

## API/CLI changes
- `bus init`

## Configuration knobs introduced
None.

## Documentation goals
- Document `bus init` as YAML-only at this stage.

## Implementation goals
- Implement CLI wiring only.

## Automated testing goals
- CLI-layer tests for deterministic output/errors.

## Acceptance criteria
- CLI delegates to core; no init logic exists in CLI code.


