# Roadmap 0.5.5 — HTTP Feature: x402 Uniqueness / Double-Booking Prevention

## Goal
Enforce uniqueness rules for x402 ingestion to prevent double booking and replay (hosted HTTP feature logic + core uniqueness primitives).

## Progress
planned

## Non-goals
- CLI command wiring.

## User-visible outcomes
- None (feature-only).

## Related previous versions
- `0.5.4` (normalization writes transactions)

## New or changed interfaces (exact names + boundaries)
Uniqueness rules (canonical):
- `docs/spec/x402.md`
- `docs/spec/micropayments.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: backend-neutral primitives to enforce uniqueness (backend-owned indexes allowed).
- **Hosted HTTP**: defines and enforces the uniqueness rules.

## Data model changes
Backend-defined (may require backend-owned indexes).

## API/CLI changes
None (feature-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure uniqueness rules are documented in `docs/spec/x402.md` / `docs/spec/micropayments.md`.

## Implementation goals
- Implement uniqueness enforcement:
  - `(network, transactionId)` when available
  - `(sourceType, sourceRef)` when available

## Automated testing goals
- Add unit tests demonstrating duplicate ingests are rejected deterministically.

## Acceptance criteria
- Double booking is prevented per the spec with deterministic errors.
