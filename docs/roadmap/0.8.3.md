# Roadmap 0.5.3 — HTTP Feature: x402 Parse Headers Operation

## Goal
Implement x402 header parsing/validation (base64 JSON) as hosted HTTP feature logic.

## Progress
planned

## Non-goals
- CLI command wiring (separate patch).
- Normalization into transactions (separate patch).

## User-visible outcomes
- None (feature-only).

## Related previous versions
- `0.5.1` (x402 config exists)

## New or changed interfaces (exact names + boundaries)
x402 header parsing rules (canonical):
- `docs/spec/x402.md`

## Module responsibilities (what belongs in a module vs core)
- **Core**: generic validation/error conventions.
- **Hosted HTTP**: parses/validates header payloads into structured artifacts.

## Data model changes
None.

## API/CLI changes
None (feature-only patch).

## Configuration knobs introduced
None.

## Documentation goals
- Ensure header parsing rules are documented in `docs/spec/x402.md`.

## Implementation goals
- Implement base64 decode + JSON parse + required-field validation for `X-PAYMENT` and `X-PAYMENT-RESPONSE`.

## Automated testing goals
- Add unit tests for bad base64/JSON errors and deterministic parsing.

## Acceptance criteria
- Parsing is deterministic and rejects invalid inputs with clear errors.
